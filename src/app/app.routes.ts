import { Routes } from '@angular/router';
import { HeroComponent } from './mainComponents/hero/hero.component';
import { MainPageComponent } from './mainComponents/main-page/main-page.component';

export const routes: Routes = [
    { path: '', component: MainPageComponent }
];
